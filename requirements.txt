# ============================================
# PDBot Requirements - v1.4.0
# Enterprise RAG with Phase 2 Reliability Improvements
# ============================================

# ---- Frontend & UI ----
streamlit>=1.36.0              # Interactive web UI framework for chatbot interface
requests>=2.32.0               # HTTP library for API calls (Ollama, Qdrant health checks)

# ---- Document Processing ----
pypdf>=4.2.0                   # PDF text extraction for manual ingestion (fallback parser)
PyMuPDF>=1.24.0                # Primary PDF parser (better OCR and table handling for annexures)

# ---- LLM Backend ----
transformers>=4.44.2           # Hugging Face transformers for tokenization and embeddings
torch==2.5.1                   # PyTorch deep learning framework (pinned - v2.9+ has meta tensor bug)

# ---- RAG Pipeline ----
langchain>=0.2.0               # RAG orchestration framework (document loaders, chains)
langchain-text-splitters>=0.2.0  # Sentence-level text chunking for granular retrieval
langchain-community>=0.2.0     # Community integrations (Qdrant, Ollama connectors)

# ---- NLP & Embeddings ----
nltk>=3.8.1                    # Natural Language Toolkit (sentence tokenization - punkt)
sentence-transformers>=3.0.0   # Semantic embeddings + cross-encoder reranking
                               # Models: all-MiniLM-L6-v2 (embeddings, 384d)
                               #         cross-encoder/ms-marco-MiniLM-L-6-v2 (reranker)

# ---- Vector Database ----
qdrant-client>=1.9.0           # Qdrant vector DB client for semantic search

# ---- Compatibility Fixes ----
# Note: Keras 3 is not compatible with sentence-transformers
# If keras is installed, uninstall it with: pip uninstall -y keras
